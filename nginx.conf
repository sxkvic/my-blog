server {
    listen 80;
    server_name shiliuge.chat www.shiliuge.chat;

    # 强制跳转 HTTPS (建议配置 SSL 后开启)
    # return 301 https://$host$request_uri;

    # 网站根目录，请根据实际上传位置修改
    root /var/www/shiliuge.chat;
    index index.html;

    # Gzip 压缩，加速访问
    gzip on;
    gzip_min_length 1k;
    gzip_buffers 4 16k;
    gzip_comp_level 5;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
    gzip_vary on;

    location / {
        # Vite + Vue Router history 模式回退
        try_files $uri $uri/ /index.html;
    }

    # 缓存静态资源配置
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|webp)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # 错误页面
    error_page 404 /index.html;
}

# HTTPS 配置示例 (获取证书后取消注释)
# server {
#     listen 443 ssl http2;
#     server_name shiliuge.chat www.shiliuge.chat;
#     root /var/www/shiliuge.chat;
#     index index.html;
#
#     ssl_certificate /etc/nginx/ssl/shiliuge.chat.crt;
#     ssl_certificate_key /etc/nginx/ssl/shiliuge.chat.key;
#     ssl_session_timeout 5m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#     # ... 其他配置同上
# }
